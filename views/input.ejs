<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Input Games</title>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.6/css/materialize.min.css">
    <link rel="stylesheet" href="css/input.css">

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.0.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.6/js/materialize.min.js"></script>


  </head>
  <body>

    <div class="container">
      <h2>Input Games</h2>


      <div class="card">
        <form id="addgame">

          <h5>Add a game</h5>
          <div class="input-field">
            <input id="name" type="text" class="validate">
            <label for="name">Name</label>
          </div>
          <div class="input-field">
            <input id="address" type="text" class="validate">
            <label for="address">Address</label>
          </div>
          <div class="input-field">
            <input id="image_url" type="text" class="validate">
            <label for="image_url">Image URL</label>
          </div>
          <div class="input-field">
            <input id="latlong" type="text" class="validate">
            <label for="latlong">Latitude & Longitude (latitude,longitude)</label>
          </div>
          <div class="input-field">
            <input id="when" type="date" class="validate">
          </div>

          <input type="submit" class="btn" value="Submit"/>

        </form>
      </div>
    </div>

    <div class="container">
      <h2>Current Games</h2>
      <div id="games">

      </div>
    </div>

    <script type="text/javascript">

      $(document).ready(function(){
        $.get('/games', function(data, status){
          data.forEach(function(item){
            $('#games').append("<h5>" + item.name + "</h5>");
            $('#games').append("<p>" + item.address + "</p>");
            $('#games').append("<p>" + item.image_url + "</p>");
            $('#games').append("<p>" + item.latlong + "</p>");
            $('#games').append("<p>" + item.when + "</p>");
            $('#games').append("<p>" + item._id + "</p>");
          })
        })
      })

      $("#addgame").submit(function(event){
        event.preventDefault();

        var name = $('#name').val();
        var address = $('#address').val();
        var image_url = $("#image_url").val();
        var latlong = $("#latlong").val();
        var when = $('#when').val();

        var data = {
          name: name,
          address: address,
          image_url: image_url,
          latlong: latlong,
          when: when
        }

        $.ajax({
          type: "POST",
          url: "/input",
          data: data,
          success: function(result){
            console.log("result");
          }
        })
        Materialize.toast('Game added', 4000);
      });

    </script>

  </body>
</html>
