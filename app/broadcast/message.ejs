
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <a href="dashboard"><title><%=s.company.name%></title></a>

    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link href="css/dashboardCoded.css" rel="stylesheet">
  </head>

  <body>

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="dashboard.<%=s.company.code%>"><%=s.company.name%></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><a href="dashboard.<%=s.company.code%>">Overview</a></li>
            <li><a href="events.<%=s.company.code%>">Events</a></li>
            <li><a href="message.<%=s.company.code%>">Message</a></li>
            <li><a href="users.<%=s.company.code%>">Members</a></li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container-fluid">
      <div class="row">
        <div class="main">
          <a href="dashboard"><h1 class="page-header"><%=s.company.name%></h1></a>

          <h2 class="sub-header">Message</h2>

           <form id="addevent">

              <div class="form-group">
                <label for="textarea_message">
                  Message Text
                </label>
                <textarea class="form-control" rows="3" placeholder="Message Text"
                 name="message" id="message"></textarea>
                <br/>
                <textarea class="form-control" rows="3" placeholder="List of Id's (Leave blank to message everyone)"
                 name="ids" id="ids"></textarea>
              </div>
              <input type="button" class="btn btn-success btn-block" value="Send" onclick="send(message)"/>

            </form>

            <br/>

            <button type="button" class="btn btn-primary" onclick="sendEvents(events)">Send Events</button>
          </div>
        </div>
      </div>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script>window.jQuery || document.write('<script src="../../assets/js/vendor/jquery.min.js"><\/script>')</script>
    <script src="js/bootstrap.min.js"></script>
    <script type="text/javascript">

      function sendMessage(type, message, ids) {
        if (type === "events"){
          $.post(window.location.href
            + "?type=" + "events",
            (data) => {
              console.log(data);
              alert(data);
            });
        }
        else {
          $.post(window.location.href
            + "?type=" + "message"
            + "&message=" + encodeURIComponent(message)
            + "&ids=" + ids,
            (data) => {
              console.log(data);
              alert(data);
            });
        }
      }

      function send(type) {
        if (confirm("Are you sure you want to send this message?")) {
          sendMessage(type,
            $('#message').val().trim()
            $('#ids').val().trim());
        }
      }
    </script>
  </body>
</html>
